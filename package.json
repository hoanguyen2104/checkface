{
  "name": "checkface",
  "version": "1.0.0",
  "description": "Ứng dụng nhận diện cảm xúc",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "apt-get update && apt-get install -y python3 python3-pip && pip3 install deepface opencv-python-headless"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "engines": {
    "node": ">=14.x"
  }
}